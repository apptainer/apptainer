#!/bin/bash

cd `dirname $0`

# control, rules, source/include-binaries, ...
find . -name \*.withnetwork \
| while read file
do
    cp -p ${file} `dirname ${file}`/`basename ${file} .withnetwork`
done

# remove binaries
rm -f *.tar.gz

